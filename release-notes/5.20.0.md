# TPS Notebook Navigator 5.20.0

This backward-compatible minor release makes the Navigator compatible with root-level, metadata-identified TPS templates. Calendar and folder-note template choices use GCM's shared template rule when present and retain the prior folder fallback otherwise.

Unresolved Templater or double-brace expressions in a note's frontmatter title now display as the file's basename throughout Navigator's normal name path.

Minimum Obsidian: 1.11.0.

## Validation and artifacts

- Validation: focused template/name tests 12/12; full declared suite 2,792/2,792; TypeScript passed; mandatory separate production build passed; `git diff --check` passed.
- Test-vault/POC QA: the exact build was deployed to the test vault and installed/restarted in `TishOS Native POC`; root navigation visibly displays `Daily Note Template` instead of the unresolved frontmatter title expression.
- `main.js`: `b23d9672f7b07f23e24e82e9a8cf894925bd5a64c4c3766e9861865f9806709d`
- `manifest.json`: `64a7b7c0fe6aba07c604b9637220151b33cd2f1949753546b6cb9e1eb7e36966`
- `styles.css`: `bb7808f6a5087c7a8b71c9987e6ca3b677171a452ecb6c1286604fed06924116`

Tested in the test vault and the native POC; ready for the user's BRAT pull.

Known limitation: template discovery depends on GCM for tag/property modes. Without compatible GCM, Navigator deliberately uses its existing folder rule.
