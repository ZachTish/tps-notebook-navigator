# TPS Notebook Navigator 6.5.0

Navigator property visibility can now be configured alongside each GCM custom property. Update both GCM 3.4.0 and TPS Notebook Navigator 6.5.0 to use the complete integration.

- Navigator's Properties configure button, section-menu action and property-key settings action open GCM Rules & fields → Custom fields.
- Each property has separate navigation-tree, populated note-list and file-menu visibility switches, labeled with the active Navigator profile.
- Navigator remains the only persistence owner for these existing flags. GCM provides their editing surface; there is no duplicate preference store, note mutation, migration, watcher or background automation.
- Explicit all-off entries survive saving, reload and property-catalog refresh. Previously normalization discarded them and catalog import could make them visible again.
- Stale-profile edits fail before changing settings. Failed saves restore the previous in-memory values and notify the user.
- Tags, ADOLink, PR Link and existing property definitions remain intact. This release does not apply the proposed taxonomy or remove Health definitions.

## Compatibility and limits

Backward-compatible minor feature. Minimum Obsidian: 1.11.0. GCM adds `api.ui.openCustomPropertySettings()`; Navigator adds `api.propertyVisibility` version 1 (`get`/`set`). Missing/older GCM retains Navigator's standalone modal; missing/older Navigator displays an explanatory message in GCM. Global Navigator visibility switches still apply. Existing catalog import defaults remain. A Navigator-only key needs a matching GCM custom-property definition to edit it through this screen. Physical iPhone acceptance remains outstanding.

## Validation

Navigator: 3,097 tests across 271 files passed. ESLint passed with 24 existing advisory warnings and no errors. Namespace, operational/artifact identity, strings/locales and TypeScript checks passed. Final separate production builds deployed to Obsidian Plugin Test Vault, followed by targeted plugin reloads. Installed UI QA exercised Navigator Configure property keys → GCM Custom properties, expanded Status, and changed visibility with the native toggles. Turning all surfaces off survived a Navigator reload. Original test-profile keys were restored and GCM definitions were unchanged. Runtime assets match the tested source artifacts byte for byte. No notes were changed and outbound automation stayed disabled.

Tested in the test vault and ready for the user's BRAT pull. Production installation is not part of this release. Update both plugins through BRAT; public release publication does not mean production has installed them.

## SHA-256

- `main.js`: `a525b963cc7ab7a6e181a930827c81d9e7639f3a761b5fe2c9d3731b351f0f59`
- `manifest.json`: `65273280f55b98fb85335c210d6f42e02b3cec683ced98487f15c16dd789c2a9`
- `styles.css`: `d54ae4c07e9cbb27e205c9ea88562f5f175b752cd3e40ef0dd5e282f727e87f2`
