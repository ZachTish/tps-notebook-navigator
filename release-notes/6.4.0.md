Tested in Obsidian Plugin Test Vault and ready for the user's BRAT pull. Production installation is separate.

Folder notes now use a Markdown note's authored title, even when the filename differs. Matching ignores capitalization and surrounding whitespace. Untitled notes and non-Markdown resources retain filename detection; the root keeps Vault first and the actual vault name as fallback. This is a minor title-identity capability release.

Tree links, headers, menus, opening, recents, hiding and counts share the resolver and refresh after metadata edits. Counts resolve once per folder. Explicit linking, conversion, creation and folder renaming retain the intended title-based identity.

A valid title that differs from the folder-note pattern no longer matches solely because of its filename. Duplicate matching titles do not select an arbitrary note. Titles use the title property independently of the display-name-field preference; notes remain direct children of their folder. No new settings or bulk note migration. GCM 2.5.1 separately fixes filename capitalization sync.

Validation: 3,078 tests across 270 files passed; full lint had no errors, and scoped lint verified both introduced warnings were fixed (24 existing advisory warnings remain). TypeScript, locale/string usage, namespace, operational identity, artifact identity, and the separate final production build passed. The shared deploy helper deployed to the test vault; the final plugin reload passed title-only linking, list hiding/count changes, unlinking on title changes and opening the exact arbitrary-filename note from the tree. Temporary settings restored; fixtures archived.

Minimum Obsidian: 1.11.0. Physical iPhone acceptance remains pending; no production deployment was performed.

SHA-256 of the tested, shipped artifacts:

- `main.js`: `962e8e23eb0a33d027a6811df6572c385a50839c77ff279bbf5fd9bc335e1a89`
- `manifest.json`: `727519b3c4a0e45fa5be90c11fb46ed636034c3142a4ee353a149d41c3554852`
- `styles.css`: `d54ae4c07e9cbb27e205c9ea88562f5f175b752cd3e40ef0dd5e282f727e87f2`
